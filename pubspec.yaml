name: ollama_flutter_gui
description: "Ollama Flutter GUI: A modern, responsive interface for local Ollama AI models.
  Features include prompt communication, file upload, and a sleek Material Design 3 UI.
  Built with Flutter and Riverpod, enhancing local AI interaction with improved privacy and offline capabilities."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  http:
  file_picker:
  center_the_widgets:
  flutter_markdown:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:

  uses-material-design: true
