name: ollama_flutter_gui
description: "Flutter Web chat for local Ollama models with model discovery, file prompts, and a localhost-first workflow."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: 3.4.2
  http:
  file_picker:
  flutter_markdown_plus: ^1.0.7
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:

  uses-material-design: true
