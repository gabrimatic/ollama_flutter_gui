# Changelog

## Unreleased

- Move the app to Ollama's chat flow (`/api/chat`) with model discovery from `/api/tags`.
- Add endpoint editing, model refresh, clear-chat control, connection status, and response token-rate display.
- Improve file prompts with size validation, UTF-8 decoding, and explicit attached-file context.
- Update the Flutter/Riverpod implementation for current package APIs.
- Replace template project metadata with product-specific README and web manifest text.
- Add Flutter CI for analyze, tests, and web build.
